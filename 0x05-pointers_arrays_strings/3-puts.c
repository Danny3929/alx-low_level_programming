#include <stdio.h>
#include "main.h"

/**
 * _puts - function that prints a string, followed by a new line
 * @str: character
 * Return: Always
 */

void _puts(char *str)
{
	printf("%s\n", *str);
	return ('0');
}
