#include <stdio.h>
/**
 * main - how are you
 *
 * Return: ALways 0 (success)
 */

int main(void)
{
	int ch;

	for (ch = 'a' ; ch <= 'z' ; ch++);
	{
		if (ch != 'e' && ch != 'q');
		putchar(ch)
	}

	putchar('\n');

	return (0);

}
